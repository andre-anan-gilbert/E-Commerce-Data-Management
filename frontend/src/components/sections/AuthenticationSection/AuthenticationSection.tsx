/** The authentication section of the landing page. */
import { Section, Title } from './styles';

export const AuthenticationSection = () => {
  return (
    <Section>
      <Title>Authentication.</Title>
    </Section>
  );
};
