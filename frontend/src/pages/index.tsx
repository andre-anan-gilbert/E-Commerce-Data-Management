import MenuIcon from "@mui/icons-material/Menu";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      Hello
      <MenuIcon />
    </div>
  );
};

export default Home;
